# PocketQuant 교장 검토 보고서

- **작성일**: 2026-06-19
- **검토 대상**: `app/academy/curriculum/textbook.py` · `app/academy/curriculum/__init__.py` (커밋된 현재 상태 — 진행 중 diff 없음)
- **참조 정책**: AGENTS.md, 시즌 v2 마감 스냅샷(2026-06-16), 다음 작업 로드맵
- **작성자**: 교장

---

## 판정
**REVISION**

## 핵심 결론
- 교재의 **교수 본문(왜-주석·개념 설명)은 양호** — 로그수익률·블록·크로스에셋 상관·NaN 보존 순서가 선행개념 누락 없이 잘 깔려 있음.
- 다만 **로드맵·버전·작성자 라벨이 현 시즌(v2 마감) 및 협회 규정과 어긋난 채 화석화** — 학습자가 폐기된 미래(cGAN)를 정설로 오인할 커리큘럼 연결성 결함.
- 안전·비교가능성(시드 고정·hold-out 훈련 차단)은 훼손 없음 → 차단 사유 없음, 표기 정비만 필요.

## 차단 사유
- 없음 (코드·실험 재현성·hold-out 봉인 모두 정상. BLOCK 불요)

## 수정 권고

1. **도감/세계관 오염 — 폐기된 로드맵이 정설로 남음** (`curriculum/__init__.py:23-26`)
   - `v1 (다음): cGAN — 국면 라벨 조건 합성`이 아직 살아 있음. 협회 결정(2026-06-16)으로 **cGAN·웜스타트는 로드맵에서 제외**됨. 학습자가 다음 단원으로 cGAN을 기대하게 만듦 → 커리큘럼 연결성 깨짐. 현행 로드맵(시그널 풀 개편 → 교과서 재설계)으로 갱신 권고.

2. **버전 순서 오류** (`curriculum/__init__.py:24-25`)
   - `v1.1 (이번)` 다음 단계가 `v1 (다음)`로 표기 — **버전이 역행**(난이도/진행 순서 모순). 또한 프로젝트는 이미 v2 마감인데 교재 전체 프레이밍이 v1.1에 멈춰 있음. 현 시즌 기준으로 정합화 필요.

3. **작성자 라벨에 버전 잔존 — 규정 위반** (`textbook.py:39, 100, 174`)
   - `코덱스 5.5 권장`이 3곳. 협회 규정상 작성자 라벨은 **버전 빼고 모델 패밀리만**(버전추적 표기 2026-06-14 폐기). `코덱스 연구원 권장`으로 통일 권고. (단 AGENTS.md 자체가 "Codex 5.5 연구원"으로 쓰고 있어 상위 문서부터 정리돼야 함 → 협회장 확인 항목 참조)

4. **용어 일관성 — "봉인" 어휘의 시점 혼선** (`textbook.py:22`) *(경미)*
   - "사천왕 봉인 2020-07 이후 사용 금지"는 **훈련용으로는 여전히 맞음**(훈련 체육관 추가 영구 금지). 그러나 hold-out은 평가용으로 이미 봉인 해제·오염(절대규칙 #5)이라, "봉인"이라는 단어만 보면 학습자가 평가 단계까지 봉인된 줄로 오해할 수 있음. "훈련 사용 금지(평가 hold-out은 별도·오염됨)" 정도로 한정어 보강 권고.

## 협회장 확인 필요 사항
- **(A) 상위 문서의 트레이너/버전 표기 정합화 범위.** AGENTS.md가 `academy/ — 트레이너 양성`(절대규칙 #9 "트레이너 금지" 위반)과 `Codex 5.5 연구원`(버전 잔존)을 쓰고 있어, 교재만 고치면 상·하위 문서가 어긋남. **교재 표기를 상위 문서에 맞출지, 상위 문서부터 바로잡을지** 최종 판단 요청. (교육부 직접 수정 불가 사안)
- **(B) 수정 시점.** 위 1~3을 **예정된 "교과서 재설계"(다음 작업 #2, 실제 분포 상승60/하락22/횡보18 반영)에 묶어 한 번에 정비할지**, 아니면 그 전에 표기만 선반영할지 결정 요청. 재설계가 임박했다면 묶는 편이 중복 작업이 적음.

---
※ 본 보고서는 검토만 수행했으며 교재·코드를 수정하지 않음. 새 기능 제안도 포함하지 않음.
